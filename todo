BUG:
-screenshot command is unable to write
-when console is toggled, first char should not count if key is ascii.
-cursor width gives error on when choosing small char

FEATURE:
1-implement MWheel in Control Abstraction
4-add a command "log_timestamp" to enable time in log
6-add a tooltip to show predicates on input
7-add a command "debuginfo" to draw DEBUGINFO PANEL (extendable via c++)

EXTENDABLE:
-add a command "sound_stop" to stop all sounds
-add a support for copy paste
-add a HUD layer
-add a command "hud_show" to enable or not hud
-add a command "hud_color" to set HUD color

